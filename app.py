import streamlit as st
import pandas as pd
from datetime import datetime
import os

# --- 1. ตั้งค่าระบบ (Configuration) ---
st.set_page_config(page_title="ระบบทะเบียนคุมและสรุปงบประมาณ มพย.", layout="wide", page_icon="🛡️")

# ชื่อไฟล์ฐานข้อมูล
DB_FILE = "database_claims.csv"

# ข้อมูลหมวดงบประมาณ (Master Data)
BUDGET_MASTER = {
    "541010001": "หมวดส่งเสริมการวิจัย",
    "521130002": "ค่าถ่ายเอกสาร",
    "521130004": "วัสดุสิ้นเปลืองสำนักงาน",
    "531111005": "ค่ายานพาหนะ",
    "521140007": "สัมมนาภายใน",
    "531104002": "ค่าไปรษณียากร"
}

# --- 2. ฟังก์ชันช่วยคำนวณและจัดการข้อมูล ---

def get_current_date():
    """ดึงวันที่ปัจจุบันและปี พ.ศ."""
    now = datetime.now()
    thai_year = now.year + 543
    return now, thai_year, now.strftime(f"%d/%m/{thai_year}")

def get_next_doc_no():
    """คำนวณเลขที่เอกสารอัตโนมัติ (ตัดรอบ 1 ม.ค.)"""
    if not os.path.exists(DB_FILE):
        return "0203/001"
    
    try:
        df = pd.read_csv(DB_FILE)
        if df.empty: return "0203/001"
        
        # ตรวจสอบปีล่าสุดเพื่อตัดรอบ
        _, current_thai_year, _ = get_current_date()
        last_year_in_db = int(df['ปี'].iloc[-1])
        
        if current_thai_year > last_year_in_db:
            return "0203/001"
            
        last_doc = str(df['เลขที่ออก'].iloc[-1])
        last_num = int(last_doc.split('/')[-1])
        return f"0203/{last_num + 1:03d}"
    except:
        return "0203/001"

def load_data():
    """โหลดข้อมูลจาก CSV"""
    if os.path.exists(DB_FILE):
        return pd.read_csv(DB_FILE)
    return
