import streamlit as st
import pandas as pd
from datetime import datetime
import os

# --- 1. ตั้งค่าระบบ (Configuration) ---
st.set_page_config(page_title="ระบบทะเบียนคุมและสรุปงบประมาณ มพย.", layout="wide", page_icon="🛡️")

# ชื่อไฟล์ฐานข้อมูล
DB_FILE = "database_claims.csv"

# ข้อมูลหมวดงบประมาณ (Master Data)
BUDGET_MASTER = {
    "541010001": "หมวดส่งเสริมการวิจัย",
    "521130002": "ค่าถ่ายเอกสาร",
    "521130004": "วัสดุสิ้นเปลืองสำนักงาน",
    "531111005": "ค่ายานพาหนะ",
    "521140007": "สัมมนาภายใน",
    "531104002": "ค่าไปรษณียากร"
}

# ข้อมูลคณะ/หน่วยงาน (Master Data) - อัปเดตใหม่!
FACULTY_MASTER = [
    "คณะนิติศาสตร์",
    "คณะบริหารธุรกิจ",
    "วิทยาลัยสหวิทยาการ",
    "คณะพยาบาลศาสตร์แมคคอร์มิค",
    "คณะเภสัชศาสตร์",
    "วิทยาลัยนานาชาติ",
    "วิทยาลัยดุริยศิลป์",
    "วิทยาลัยพระคริสต์ธรรมแมคกิลวารี",
    "บัณฑิตวิทยาลัย",
    "สำนักการศึกษาทั่วไป"
]

# --- 2. ฟังก์ชันช่วยคำนวณและจัดการข้อมูล ---

def init_db():
    """สร้างไฟล์ฐานข้อมูลถ้ายังไม่มี"""
    if not os.path.exists(DB_FILE):
        columns = ["NO", "เลขที่ออก", "วัน", "เดือน", "ปี", "ผู้ลงนาม", "ถึง", "เรื่อง", 
                   "คณะ", "หัวหน้าโครงการวิจัย", "ผู้ประสาน", "เงินที่อนุมัติ", 
                   "จำนวนเงิน", "ชื่อโครงการ", "รหัสหมวด", "บันทึกเมื่อ"]
        df = pd.DataFrame(columns=columns)
        df.to_csv(DB_FILE, index=False, encoding='utf-8-sig')

def get_current_date():
    """ดึงวันที่ปัจจุบันและปี พ.ศ."""
    now = datetime.now()
    thai_year = now.year + 543
    return now, thai_year, now.strftime(f"%d/%m/{thai_year}")

def get_next_doc_no():
    """คำนวณเลขที่เอกสารอัตโนมัติ (ตัดรอบ 1 ม.ค.)"""
    try:
        if not os.path.exists(DB_FILE):
            return "0203/001"
        
        df = pd.read_csv(DB_FILE, encoding='utf-8-sig')
        if df.empty: return "0203/001"
        
        # แปลงข้อมูลปีให้เป็นตัวเลขที่ปลอดภัย
        df['ปี'] = pd.to_numeric(df['ปี'], errors='coerce').fillna(0).astype(int)
        
        _, current_thai_year, _ = get_current_date()
        last_year_in_db = df['ปี'].iloc[-1]
        
        # ถ้าขึ้นปีใหม่ หรือ ข้อมูลปีผิดปกติ ให้เริ่ม 001
        if current_thai_year > last_year_in_db:
            return "0203/001"
            
        last_doc = str(df['เลขที่ออก'].iloc[-1])
        if "/" in last_doc:
            last_num = int(last_doc.split('/')[-1])
            return f"0203/{last_num + 1:03d}"
        else:
            return "0203/001"
            
    except Exception as e:
        return "0203/001"

def load_data():
    """โหลดข้อมูลจาก CSV แบบปลอดภัย"""
    if os.path.exists(DB_FILE):
        try:
            return pd.read_csv(DB_FILE, encoding='utf-8-sig')
        except:
            return pd.DataFrame()
    return pd.DataFrame()

def prepare_summary_data(df):
    """คำนวณคอลัมน์ปีต่างๆ โดยจัดการ Error ให้"""
    if df.empty: return df
    
    df['ปี'] = pd.to_numeric(df['ปี'], errors='coerce').fillna(0).astype(int)
    df['เดือน'] = pd.to_numeric(df['เดือน'], errors='coerce').fillna(0).astype(int)
    df['จำนวนเงิน'] = pd.to_numeric(df['จำนวนเงิน'], errors='coerce').fillna(0.0)

    # คำนวณปีงบประมาณ/ปีการศึกษา
    df['ปีงบประมาณ'] = df.apply(lambda x: x['ปี'] + 1 if x['เดือน'] >= 8 else x['ปี'], axis=1)
    df['ปีการศึกษา'] = df.apply(lambda x: x['ปี'] if x['เดือน'] >= 6 else x['ปี'] - 1, axis=1)
    df['ปีปฏิทิน'] = df['ปี']

    return df

# --- เริ่มต้นทำงาน ---
init_db()

# --- 3. ส่วนแสดงผล (UI) ---

st.sidebar.title("🛡️ เมนูหลัก")
menu = st.sidebar.radio("เลือกหน้าทำงาน", ["📝 บันทึกตั้งเบิก (หน้าหลัก)", "📊 สรุปรายงานการใช้งบ"])

# ปุ่มฉุกเฉิน
st.sidebar.markdown("---")
if st.sidebar.button("⚠️ ล้างฐานข้อมูล (Reset)"):
    if os.path.exists(DB_FILE):
        os.remove(DB_FILE)
        init_db()
        st.sidebar.success("ล้างข้อมูลเรียบร้อย! โปรดรีเฟรชหน้าเว็บ")

# --- หน้าที่ 1: บันทึกตั้งเบิก ---
if menu == "📝 บันทึกตั้งเบิก (หน้าหลัก)":
    try:
        now, thai_year, date_str = get_current_date()
        
        st.title("📝 ระบบบันทึกขออนุมัติเบิกจ่าย")
        st.info(f"📅 วันที่: {date_str} | ปีงบประมาณ พ.ศ. {thai_year}")

        next_doc = get_next_doc_no()
        st.subheader(f"เลขที่เอกสารที่จะออก: :blue[{next_doc}]")

        with st.form("entry_form", clear_on_submit=True):
            subject = st.text_input("เรื่อง (Subject)")
            
            c1, c2 = st.columns(2)
            with c1:
                project_name = st.text_input("ชื่อโครงการวิจัย")
                total_budget = st.number_input("งบประมาณทั้งโครงการ", min_value=0.0, format="%.2f")
                
                # เปลี่ยนเป็น Dropdown คณะ
                faculty = st.selectbox("คณะ / หน่วยงาน", FACULTY_MASTER)
            
            with c2:
                leader_name = st.text_input("ชื่อหัวหน้าโครงการ")
                claim_amount = st.number_input("จำนวนเงินที่เบิกครั้งนี้", min_value=0.0, format="%.2f")
                
                budget_choice = st.selectbox(
                    "หมวดงบประมาณ", 
                    options=list(BUDGET_MASTER.keys()),
                    format_func=lambda x: f"{x} - {BUDGET_MASTER[x]}"
                )

            to_who = st.text_input("เรียน", value="รองอธิการบดีฝ่ายวิชาการและวิจัย")
            
            submitted = st.form_submit_button("✅ บันทึกข้อมูลและออกเลขที่เอกสาร")

        if submitted:
            if not subject or not project_name:
                st.error("⚠️ กรุณากรอก 'เรื่อง' และ 'ชื่อโครงการ' ให้ครบถ้วน")
            else:
                full_budget_text = f"{budget_choice} {BUDGET_MASTER[budget_choice]}"
                
                new_data = {
                    "NO": "", "เลขที่ออก": next_doc,
                    "วัน": now.day, "เดือน": now.month, "ปี": thai_year,
                    "ผู้ลงนาม": "ผู้อำนวยการ", "ถึง": to_who, "เรื่อง": subject,
                    "คณะ": faculty, "หัวหน้าโครงการวิจัย": leader_name,
                    "ผู้ประสาน": "", "เงินที่อนุมัติ": total_budget,
                    "จำนวนเงิน": claim_amount, "ชื่อโครงการ": project_name,
                    "รหัสหมวด": full_budget_text,
                    "บันทึกเมื่อ": datetime.now().strftime("%Y-%m-%d %H:%M:%S")
                }
                
                df_new = pd.DataFrame([new_data])
                df_new.to_csv(DB_FILE, mode='a', index=False, header=False, encoding='utf-8-sig')
                
                st.balloons()
                st.success(f"บันทึกข้อมูลสำเร็จ! เลขที่เอกสาร: {next_doc}")
                st.rerun()

    except Exception as e:
        st.error(f"เกิดข้อผิดพลาด: {e}")

# --- หน้าที่ 2: สรุปรายงาน (Dashboard) ---
elif menu == "📊 สรุปรายงานการใช้งบ":
    try:
        st.title("📊 Dashboard สรุปการใช้งบประมาณ")
        
        raw_df = load_data()
        
        if raw_df.empty:
            st.warning("📭 ยังไม่มีข้อมูลในระบบ กรุณาบันทึกข้อมูลที่หน้าแรกก่อนครับ")
        else:
            df = prepare_summary_data(raw_df)
            
            with st.container():
                st.markdown("### 🔍 เลือกเงื่อนไขการสรุปผล")
                col_filter1, col_filter2 = st.columns(2)
                
                with col_filter1:
                    year_type = st.radio(
                        "ประเภทปีที่ต้องการดู:",
                        ["ปีงบประมาณ (ส.ค. - ก.ค.)", "ปีการศึกษา (มิ.ย. - พ.ค.)", "ปีปฏิทิน (ม.ค. - ธ.ค.)"]
                    )
                
                map_year_col = {
                    "ปีงบประมาณ (ส.ค. - ก.ค.)": "ปีงบประมาณ",
                    "ปีการศึกษา (มิ.ย. - พ.ค.)": "ปีการศึกษา",
                    "ปีปฏิทิน (ม.ค. - ธ.ค.)": "ปีปฏิทิน"
                }
                selected_col = map_year_col[year_type]
                
                with col_filter2:
                    unique_years = df[selected_col].unique()
                    available_years = sorted(unique_years, reverse=True) if len(unique_years) > 0 else [datetime.now().year + 543]
                    selected_year = st.selectbox(f"เลือกปี พ.ศ.", available_years)

            filtered_df = df[df[selected_col] == selected_year]
            
            total_sum = filtered_df['จำนวนเงิน'].sum()
            doc_count = len(filtered_df)
            
            st.markdown("---")
            m1, m2, m3 = st.columns(3)
            m1.metric("💰 ยอดเบิกจ่ายรวม", f"{total_sum:,.2f} บาท")
            m2.metric("📄 จำนวนเอกสาร", f"{doc_count} ฉบับ")
            m3.metric("📅 ข้อมูลประจำปี", f"{selected_year}")
            
            c1, c2 = st.columns(2)
            
            with c1:
                st.subheader("1. สรุปตามหมวดงบประมาณ")
                if not filtered_df.empty:
                    group_budget = filtered_df.groupby("รหัสหมวด")['จำนวนเงิน'].sum().reset_index()
                    group_budget = group_budget.sort_values("จำนวนเงิน", ascending=False)
                    st.dataframe(group_budget.style.format({"จำนวนเงิน": "{:,.2f}"}), use_container_width=True, hide_index=True)
                else:
                    st.info("ไม่มีข้อมูล")

            with c2:
                st.subheader("2. สรุปตามคณะ/หน่วยงาน")
                if not filtered_df.empty:
                    # ตรงนี้จะสวยงามขึ้นเพราะชื่อคณะเป็นมาตรฐานเดียวกันแล้ว
                    group_faculty = filtered_df.groupby("คณะ")['จำนวนเงิน'].sum().reset_index()
                    group_faculty = group_faculty.sort_values("จำนวนเงิน", ascending=False)
                    st.dataframe(group_faculty.style.format({"จำนวนเงิน": "{:,.2f}"}), use_container_width=True, hide_index=True)
                else:
                    st.info("ไม่มีข้อมูล")

            st.markdown("---")
            if os.path.exists(DB_FILE):
                with open(DB_FILE, "rb") as f:
                    st.download_button("📥 ดาวน์โหลดข้อมูลทั้งหมด (CSV)", f, "database_claims.csv", "text/csv")

    except Exception as e:
        st.error(f"เกิดข้อผิดพลาด: {e}")
